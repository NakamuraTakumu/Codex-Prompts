---
name: skill-writer
description: skill の新規作成、既存 skill への内容追加、既存 skill のレビューを、冗長性の排除、Markdown による構造化、命名の厳密化、情報保持の観点で支援する。
---

# Skill Writer

## 使用場面

- **新規作成**: 新しい skill を、目的、適用対象、入力、手順、出力、検証条件に分けて作る。
- **内容追加**: 既存 skill の構造を崩さず、新しい内容を適切なモジュール、record、section に追加する。
- **既存レビュー**: 既存 skill の冗長性、構造の混線、命名の曖昧さ、task-specific な混入、情報落ちを確認する。

## Workflow

### 新規作成

1. 要件確認で固定した目的、使用場面、対象読者を前提にする。
2. **冗長性の排除、構造化、厳密化**を意識しユーザーの指示を一旦まとめる。
3. ユーザーが納得したら、`skill-creator` を使って skill template を適用し作成する。
4. 完成した skill を「skillレビュー」の手順で確認する。

### 内容追加

1. ユーザーに対象 skill と追加内容を受け取る。
2. **冗長性の排除、構造化、厳密化**をもとに、その内容をどこに追加すべきかを考える。
3. 追加内容を反映する。
4. 変更した skill を「skillレビュー」の手順で確認する。

### 既存レビュー

1. ユーザーから対象 skill を受け取る。
2. 対象 skill を「skillレビュー」の手順で確認する。

## Core Principles

- **冗長性の排除**: 別の主張から推測できるような冗長な指示を書かない。同じ指示を繰り返さない。
- **構造化**: 自然言語的な流暢さよりソフトウェアエンジニアリング的な構造化を優先する。
- **厳密化**: 名称などを自然言語の曖昧性に依存しない。

## Markdown でよく使う機能と呼び名

### Markdown / CommonMark 寄りの呼び名

- **front matter**: ファイル先頭の `---` で囲まれた metadata。
- **ATX heading**: `#`、`##`、`###` で作る見出し。
- **section**: ATX heading で区切られた文書上のまとまり。
- **unordered list**: `-` で並べる list。
- **ordered list**: `1.`、`2.` で並べる list。
- **child list**: list item の下に字下げして置く list。
- **code span**: 文中の code、path、key、command を示す `` `...` ``。
- **fenced code block**: 複数行の code、schema、template、出力例を示す ```` ``` ````。
- **link**: 関連 file、reference、URL への参照。
- **table**: 対応関係や比較を列と行で示す表。
- **block quote**: 引用や外部文面の抜粋を示す `>`。
- **HTML comment**: 読者に表示しない補足や作業メモを示す `<!-- ... -->`。

### skill 内で定義する構造名

- **description item**: `**ラベル**: 内容` の形で、対象、条件、例などの属性を示す list item。
- **unordered description**: description item を unordered list として並べたもの。
- **ordered description**: description item を ordered list として並べたもの。
- **record**: 1 つの対象を親 list item とし、その child list に `**field**: value` 形式の description item を並べた構造。
  - **例**:
    - **target1**:
      - **field A**: value A
      - **field B**: value B
    - **target2**:
      - **field A**: value A
      - **field B**: value B

## 冗長性の排除

- 下記の書く内容と、その目的のどれにも該当しない指示は勝手に書かない。
- 読みにくい細分化、機械的すぎる記法、Markdown の乱用は避ける。
- 同じ指示を異なる箇所に何度も書かない。
  - 人間よりもLLMは影響を受けやすい。

### 書く内容

- プロンプトとして記述するのは、基本的に入力、手順、出力の指定。
  - **入力**: 参照する情報、受け取る値、前提、不明情報の扱い。
  - **手順**: 調査、判断、変換、編集、レビューなどの進め方。
  - **出力**: 形式、粒度、保存先、返答内容、schema、検証条件。

### 書く目的

- プロンプトを記述する目的は、動作の不確実性の減少と、推論コストの減少。
  - **動作の不確実性の減少**: 判断基準、責務境界、禁止事項、出力契約の明示。
  - **推論コストの減少**: 読むべき情報、使うべき tool、参照すべき file、確認すべき条件の明示。

## 構造化

- 指示はMarkdownを使って構造化する。
- 自然言語としての文構造は重視しない
  - 文章を description item で置き換えるなど。
  
### 分解とモジュール化

- **モジュール**: 指示文書の中で、独立した責務、判断、契約のいずれかを担う記述上の構成要素。
- 大きな指示を、意味のあるモジュールに分ける。
- 各モジュールでは責務、判断、契約を明記する。

#### 分離の種類

- **分離の種類**:
  - **独立な分離**: 互いに前後関係や参照関係を持たない関心を分ける。
  - **依存のある分離**: モジュールどうしの前後関係、参照関係、前提関係を明示する。

#### Markdown による分離

- **section**:
  - **分離の種類**: 独立な分離、依存のある分離。
  - **用途**: 独立した責務、関心、workflow phase、または順序や参照関係を持つまとまりを分ける。
  - **具体例**: 調査、判断、編集、検証、出力を別 section に分ける。
  - **避ける例**: 責務境界のない細かい section を増やす。
- **unordered list**:
  - **分離の種類**: 独立な分離。
  - **用途**: 同じ型の項目、選択肢、チェックリスト、分類を並べる。
  - **具体例**: 同じ predicate を共有する項目を並べる。
  - **避ける例**: 原則、例外、補足、例示を同じ description を使わない unordered list に混ぜる。
- **unordered description**:
  - **分離の種類**: 独立な分離。
  - **用途**: 1 つの対象に対する属性を並べる。
  - **具体例**: `**対象**`、`**使用構文**`、`**具体例**`、`**避ける例**`
  - **避ける例**: 属性名を付けずに異なる役割の説明を並べる。
- **ordered list**:
  - **分離の種類**: 依存のある分離。
  - **用途**: 時系列、実行手順、優先順位、評価段階を並べる。
  - **具体例**: 前の list item が後の list item の前提になる手順を並べる。
  - **避ける例**: 依存関係が全順序でない場合に ordered list を使う。
- **ordered description**:
  - **分離の種類**: 依存のある分離。
  - **用途**: 順序を持つ各 step に、条件、入力、出力を付ける。
  - **具体例**: 各 step に `**入力**`、`**処理**`、`**出力**` を付ける。
  - **避ける例**: step 間の順序がない属性説明に ordered description を使う。
- **child list**:
  - **分離の種類**: 依存のある分離。
  - **用途**: 上位 item に従属する説明、条件、補足、例を置く。
  - **具体例**: 規則の child list に、その規則の条件や例を置く。
  - **避ける例**: 上位 item と関係しない内容を child list に置く。

### record による構造の一貫化

- 同じ種類の対象は、同じ field set を持つ record として書く。
- 同じ判断には同じ record 構造を使う。
- 類似した対象に対して、毎回別の field set を作らない。

### モジュールの一般化

- モジュールには、特定タスクの値をハードコードしない。
- 会社名、日付、path、score、field 値などの可変値は、入力、設定、template、schema、例に分離する。
- モジュール本文には、具体例ではなく再利用できる判断構造を書く。

## 厳密化

- **外部定義用語**: skill 外のツールやドキュメントで定義された名称。
  - **対象**: code、path、field name、tool name、schema key、API 名、command、英語 documentation の terminology。
  - **表記**: 外部定義用語は code span で示す。
    - **例**: `schema key`
    - **例**: `field name`
    - **例**: `command`
- **内部定義用語**: skill 内で定義された名称。
  - **対象**: 概念名、責務名、判断基準名、workflow 名、step 名、入力名、出力名、schema 上の意味名、review 観点名、scoring 観点名。
  - **表記**: 内部定義用語は bold で示す。
    - **例**: **モジュール**
    - **例**: **関心分離**
    - **例**: **出力契約**

#### 内部定義用語の命名方法

- skill 内で定義と解釈を明示してから使う。
- 名称にはその型と意味を含める (型/意味)
  - **例**: `入力情報一覧`: 一覧 / 入力情報。
  - **例**: `required inputs`: inputs / required。
  - **例**: `判断基準`: 基準 / 判断。
  - **例**: `output_contract`: contract / output。
  - **例**: `保存先 path`: path / 保存先。
  - **例**: `review checklist`: checklist / review。
- 同じ名称に複数の意味を持たせない。
  - 抽象度の高い語を複数の概念に使い回さない。
  - 文脈ごとに意味が変わる名称にしない。
- 同じ意味に複数の名称を持たせない。
  - 短縮形と正式名称を混ぜない。
  - 英語か日本語のどちらかに統一する。

#### 構造と名称

- 名称だけで意味境界を示すのではなく、section、record、schema、template の構造も使って意味境界を示す。
- section、schema、template などでスコープが明示されている場合だけ、意味境界を損なわない範囲で、そのスコープ内で自明な語を名称から省いてよい。
- 同じ record の field 名は、その record の対象が固定されている前提で命名してよい。
- section 名、record の親 item、field 名で同じ情報を重複させない。

### 汎用手順

#### 要件確認

- **確認項目**:
  - **目的**: skill が解決する問題。
  - **使用場面**: skill を使うタイミング。
  - **対象読者または実行者**: skill を読む、または実行する主体。
  - **入力**: ユーザーから受け取る値、参照する file、前提。
  - **出力**: 返答形式、編集対象、保存先、検証条件。
  - **禁止事項**: skill が行ってはいけないこと。
- **確認方法**:
  - 既存情報から合理的に決められる場合は、仮案を作ってユーザーに確認する。
  - skill の目的、使用場面、出力契約が不明な場合は、作成前に質問する。

#### skillレビュー

- Core Principles、構造化、厳密化、情報保持の各 section に照らして確認する。
- 削除や抽象化を行った場合は、情報落ちがないか確認する。
- 既存 skill を編集した場合は、`git diff` などで差分を確認する。
